# T-Metrics SQL Agent V2 依赖包

# 必需依赖
# 核心Agent框架
agno>=0.0.1

# 数据处理
pandas>=1.5.0
numpy>=1.21.0

# HTTP客户端（用于SQL验证）
requests>=2.28.0

# 可选依赖 - Timeplus/Proton连接
# 注意：需要使用特殊的index-url安装
# pip install proton-driver --extra-index-url https://d.timeplus.com/simple/
# proton-driver  # 如果手动安装可以取消注释

# 可选依赖 - 额外的AI模型提供商
# 只在需要使用相应模型时安装
# openai>=1.0.0     # for OpenAI GPT models
# groq>=0.4.0       # for Groq Llama models

# 开发和测试依赖（可选）
# pytest>=7.0.0
# black>=22.0.0
# flake8>=4.0.0